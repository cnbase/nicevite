/**
 * vite plugins
 */
const AppPlugins = []

/**
 * vite resolve 配置
 */
const AppResolve = {}

/**
 * vite css 配置
 */
 const AppCss = {}

/**
 * 自定义 rollupOptions 参数
 */
const AppRollupOptions = {}

module.exports = {
    AppPlugins,
    AppResolve,
    AppCss,
    AppRollupOptions,
}